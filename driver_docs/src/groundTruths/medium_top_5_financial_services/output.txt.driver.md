# Purpose
The provided content outlines a structured approach to constructing a SQL query aimed at identifying the top 5 stocks in the financial services industry by market capitalization. It details a step-by-step methodology for generating subqueries, joining relevant tables, and performing necessary calculations and filters to ensure accurate and efficient data retrieval. The instructions emphasize the importance of grouping and aggregating data correctly to avoid common SQL errors, such as referencing ungrouped columns. The final output is a JSON object containing the SQL query, which selects relevant fields from the `stockindustries` and `price_data` tables, filters for financial services stocks, and orders the results by market capitalization in descending order, limiting the output to the top 5 entries.
