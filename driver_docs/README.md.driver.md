# Purpose
The provided content is a comprehensive README file for a software project named "Promptimizer," which is an automated AI-powered framework designed to optimize prompts for large language models (LLMs) using genetic algorithms and machine learning techniques. This file serves as a guide for setting up, configuring, and running the software, detailing prerequisites such as Node.js, TypeScript, Python, and MongoDB, as well as environment variables necessary for integration with various APIs and databases. The README outlines the project's features, including prompt optimization through genetic algorithms, and provides instructions for installation, configuration, and execution of the software components. It also includes guidance on generating ground truths, creating scoring heuristics, and setting up alternative models like Ollama. The file is crucial for users and developers to understand the setup and functionality of the Promptimizer framework, ensuring they can effectively utilize and contribute to the project.
# Content Summary
The provided document is a comprehensive guide for setting up and using the "Promptimizer" framework, an automated AI-powered system designed to optimize prompts for large language models (LLMs) using genetic algorithms and machine learning techniques. The framework is particularly exemplified with an AI-driven stock screening application, but it is adaptable for various LLM prompts. The document outlines the project's features, prerequisites, installation steps, and usage instructions.

Key features of the framework include genetic algorithm-based optimization, population management with crossover and mutation operations, automated evaluation of prompt performance, and multi-generational evolution of prompts. Users can customize parameters such as population size and the number of generations to tailor the optimization process to their needs.

To set up the project, developers need to ensure they have Node.js and Python installed, along with specific dependencies. The document provides detailed instructions for creating necessary configuration files (`inputs.ts` and `additionalSystemPrompts.ts`) and setting environment variables in a `.env` file. These variables include API keys for services like Anthropic and OpenAI, database connection strings, and model names.

The installation process involves setting up Node.js and Python dependencies, as well as installing and configuring MongoDB for local database management. The document also provides guidance on generating ground truths for model training, creating a scoring heuristic using a "Prompt Evaluator," and setting up alternative models like Ollama.

For running the TypeScript scripts, the document explains how to use `ts-node` or compile TypeScript to JavaScript for execution. Additionally, it includes instructions for visualizing prompt performance over time using a Python script (`graph.py`), which generates graphs to track training and validation fitness across generations.

Overall, the document serves as a detailed manual for developers to effectively implement and utilize the Promptimizer framework, ensuring they can optimize LLM prompts efficiently and visualize improvements in performance.
